using System.Collections;
using System.Collections.Generic;
//using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

public class FileStorageTest
{
    // A Test behaves as an ordinary method
    //[Test]
    //public void JsonStorageTest()
    //{
    //    // Use the Assert class to test conditions
    //    var newAssetsContainer = new AssetsContainer();
    //    newAssetsContainer.InitContainer();

    //    bool result = JsonStorage.WriteJson(newAssetsContainer, PathStorage.ASSETS_LISTUP);
    //    Assert.IsTrue(result);

    //    var readContainer = JsonStorage.ReadJson<AssetsContainer>(PathStorage.ASSETS_LISTUP);
    //    Assert.IsNotNull(readContainer);
    //}
}
